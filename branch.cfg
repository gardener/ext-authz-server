cfgs:
  default:
    branches: ['master']
  release-branches:
    index: 1
    branches: ['release-.*']
    inherit:
      ext-authz-server:
        jobs:
          release:
            traits:
              release:
                nextversion: 'bump_patch'
